// Seed fixture: 5 buildings, 53 systems, 1599 tasks — zero API calls.
// Built 2026-07-03 from acceptance_v5_out.json + DOCX schedule parse via runStage3.
// Import in tests or call window.__seedPlatform() in browser dev console.

import fixtureData from '../__fixtures__/seedPlatform.json'

export const seedFixture = fixtureData
